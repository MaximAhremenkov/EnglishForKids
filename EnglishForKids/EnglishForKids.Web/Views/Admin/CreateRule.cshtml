@model EnglishForKids.Web.ViewModels.RuleAdminViewModel
@{
    ViewData["Title"] = "Создание правила";
    Layout = "_LayoutAdmin";
}

<div class="container-fluid mt-4">
    <div class="row">
        <div class="col-md-8 mx-auto">
            <div class="card">
                <div class="card-header">
                    <h4>➕ Создание нового правила</h4>
                </div>
                <div class="card-body">
                    <form method="post">
                        <div asp-validation-summary="ModelOnly" class="text-danger"></div>

                        <div class="mb-3">
                            <label asp-for="TopicId" class="form-label">Тема</label>
                            <select asp-for="TopicId" class="form-select" required>
                                <option value="">Выберите тему</option>
                                @foreach (var topic in Model.Topics)
                                {
                                    <option value="@topic.Id">@topic.Title</option>
                                }
                            </select>
                            <span asp-validation-for="TopicId" class="text-danger"></span>
                        </div>

                        <div class="mb-3">
                            <label asp-for="Title" class="form-label">Название правила</label>
                            <input asp-for="Title" class="form-control" required />
                            <span asp-validation-for="Title" class="text-danger"></span>
                        </div>

                        <div class="mb-3">
                            <label asp-for="TheoryText" class="form-label">Текст теории</label>
                            <textarea asp-for="TheoryText" class="form-control" rows="6"
                                      placeholder="Можно использовать HTML-теги для форматирования"></textarea>
                            <small class="text-muted">
                                💡 Поддерживается HTML: <code>&lt;b&gt;жирный&lt;/b&gt;</code>,
                                <code>&lt;ul&gt;&lt;li&gt;список&lt;/li&gt;&lt;/ul&gt;</code>
                            </small>
                            <span asp-validation-for="TheoryText" class="text-danger"></span>
                        </div>

                        <div class="mb-3">
                            <label asp-for="ExampleText" class="form-label">Примеры</label>
                            <textarea asp-for="ExampleText" class="form-control" rows="3"
                                      placeholder="Например: I am a student."></textarea>
                            <span asp-validation-for="ExampleText" class="text-danger"></span>
                        </div>

                        <div class="mb-3">
                            <label asp-for="VideoUrl" class="form-label">URL видео (YouTube)</label>
                            <input asp-for="VideoUrl" class="form-control"
                                   placeholder="https://www.youtube.com/watch?v=..." />
                            <small class="text-muted">Необязательно. Можно добавить ссылку на обучающее видео</small>
                        </div>

                        <div class="mt-4">
                            <button type="submit" class="btn btn-success btn-lg">✅ Создать</button>
                            <a href="/Admin/Rules" class="btn btn-secondary btn-lg">↩️ Отмена</a>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}